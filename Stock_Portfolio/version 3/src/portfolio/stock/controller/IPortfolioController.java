package portfolio.stock.controller;

/**
 * The interface defines the methods offered by the controller of the MVC program.
 */
public interface IPortfolioController {
  /**
   * The method performs all the operations by interacting with the model and the view. This method
   * is called by the main method to start the interactive program.
   */
  void goController();
}
